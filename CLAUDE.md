# JEWEL SURVIVOR — 開発引き継ぎ資料

このファイルは、次に開発を担当する AI（Claude Code など）向けの引き継ぎ資料です。
ユーザーに同じ説明を繰り返させないよう、**作業を始める前に必ず最後まで読んでください**。
プレイヤー向けの紹介は README.md、ジュエルパワーの対応表は docs/GEMS.md にあります。

---

## 1. ユーザーについて（最重要）

- **応答は敬語（です・ます）で**。ユーザー本人はくだけた口調（「w」「草」など）で書いてくるが、こちらは敬語を保つ。
- ユーザーは **21 歳の成人男性**で、自分で遊ぶために作っている。**子ども向けの配慮は一切不要**。ひらがなだらけの表記、幼い言い回し、ファンシーな表現は使わず、漢字を使った普通の日本語で書く。
- GitHub にはあまり詳しくない。PR の作成・マージ・デプロイは AI 側で済ませ、「公開版に反映済み」まで持っていくのが通常の流れ（§6）。
- **公開はしない（個人で遊ぶだけ）**。リポジトリと Pages は public だが、宣伝するつもりはない。権利表記は README の末尾だけに置き、**ゲーム画面には出さない**。
- 変更の要望は短い箇条書きで、まとめて届くことが多い。「要検証」「気のせいかも」と書かれたものは、実際にログを取って確かめてから直す（過去の「フィーバー中の経験値の挙動がおかしい」は本当に不具合だった）。
- 「難易度検証は不要」と言われたときは、ボットでのバランス確認を省き、エラーが出ないかの確認だけにする。

### 過去に受けた指摘（同じことを繰り返さない）

- **UI に意味のない文言を置かない**。以前、タイトルロゴの下に「宝石は、闇の中でこそ輝く」というキャッチコピーを入れたところ、「LLM がメモ書きや議論ログを成果物に出すクセ」と強く指摘された。飾りの一文、開発メモ、設計意図の説明などをゲーム内に書かないこと。
- 「宝石言葉」という語は使わない。**「ジュエルパワー」**で統一している（例：`ジュエルパワー「自信」`）。
- 見た目は**黒ベースでスタイリッシュ**に。宝石と光のパーティクルが映えることを優先する。ピンクやファンシーな色使いは避ける（初期版はサンリオ寄りのピンクで不評だった）。
- 自機のデザインは**白い輪郭＋黒い体（塗りつぶし）＋宝石の瞳のみ**。羽・口・頬の赤みは入れない。
- iPhone でホーム画面のアプリを削除すると、**セーブデータも消える**（Safari とは別の保存領域のため）。以前「消えない」と誤って案内し、ユーザーのデータが消えた。データに関わる案内は、確証がないなら断定しない。
- 画面を覆う攻撃エフェクト（炎・レーザー）で自機や敵が見えなくなるのは嫌われる。一方で、フレイムが「薄すぎる」とも言われた。濃さと見通しの両方に気をつける。

---

## 2. プロジェクト概要

- iPhone の Safari（ホーム画面に追加した PWA）で遊ぶ、宝石テーマのヴァンパイアサバイバーライク。
- 素の JavaScript（ES Modules）と Canvas 2D だけで作っている。**ビルド工程も依存パッケージもない**。
- 画像・音声ファイルは使わない。宝石・敵・自機の絵も、効果音・BGM も、すべてコードで生成している（アイコン PNG だけは tools/icon.html から書き出したもの）。
- 公開 URL：`https://yyrtjhgjj-byte.github.io/JEWEL-SURVIVOR/`
  - この開発環境からはプロキシでブロックされて開けない。デプロイの成否は GitHub Actions の実行結果で確認する。
- 宝石ごとの能力テーマは『ジュエルペット』（サンリオ×セガトイズ）の各ジュエルペットが担当する魔法の意味に基づく（ルビー＝勇気、サファイア＝友情、ガーネット＝愛 など）。詳しくは docs/GEMS.md。

---

## 3. ファイル構成と役割

```
index.html            HUD などの骨組み。フォントは Rajdhani と Zen Kaku Gothic New
css/style.css         全 UI のスタイル。色は :root の変数（--panel, --line, --muted, --gold, --prism など）
js/main.js            起動、メインループ（固定タイムステップ）、startGame / finishRun（セーブ反映）、
                      デバッグ用パラメータ、Service Worker の登録と自動更新
js/game.js            ゲーム本体（Game クラス）：出現、当たり判定、ボスの攻撃と移動、フィーバー、
                      経験値、ピックアップ、描画、リザルト用の集計
js/weapons.js         weaponStats()（武器の性能計算）と LOGIC（15 武器の挙動・描画）
js/data.js            GEMS / WEAPONS / PASSIVES / CHARACTERS / ENEMIES / SHOP / ACHIEVEMENTS /
                      ガチャ（GACHA_TABLE / EXCHANGE）/ MAX_WEAPONS・MAX_CHARMS
js/stages.js          STAGES（出現表・イベント・色・ギミック）、heatMods()
js/enemies.js         特殊な敵の AI（spitter, charger, bomber, wisp, phantom, prism, worm, lich, emperor）
js/hazards.js         ステージのギミック（水晶柱・溶岩・吹雪・暗闇）
js/render.js          絵の生成（gemSprite / gemIcon / coinIcon / enemySprite / drawPlayer / moonSprite / softSprite など）
js/fx.js              パーティクル（上限 500）、ダメージ表示、画面の揺れ・フラッシュ
js/audio.js           WebAudio の効果音と BGM のステップシーケンサー
js/ui.js              全画面（タイトル、ジュエル選択、ステージ選択、レベルアップ、宝箱、ガチャ、図鑑、
                      実績、工房、設定とバックアップ、リザルト、HUD、バナー）
js/input.js           画面のどこでも使えるフローティング仮想スティック（PC は WASD／矢印キー）
js/save.js            localStorage へのセーブ（キー jewel-survivor-save-v1）、バックアップの書き出し・読み込み
js/atelier.js         研磨工房のロジック（原石の等級、品質、練度、コレクションボーナス、ドロップ）
js/artifacts.js       秘宝（アルカナ相当）の定義と解放判定
js/artifact-art.js    秘宝の描き下ろしの絵（drawArtifact）。確認用に tools/artifact-preview.html で 11 種を一覧できる
js/shop-art.js        工房の強化 19 種の描き下ろしアイコン（drawShopIcon、render.js の shopIcon）。tools/shop-preview.html で一覧できる
js/gem-art.js         宝石の絵（カット別のテンプレートに GEMS の色を乗せる。GEM_CUT で宝石ごとのカット）。tools/gem-preview.html で一覧できる
js/elements.js        属性（8 種）の定義、宝石ごとの属性、発動・状態異常・揃えたボーナスの処理
js/rank.js            ユーザーレベル（ランク）：経験値、称号、獲得コインの倍率
js/howto.js           遊び方（HOW TO PLAY）の本文。項目ごとの見出しと箇条書き。**仕様を変えたらここも直す**
js/gem-facts.js       実在の宝石としての情報（モース硬度・主な産地・名前の由来・小ネタ）。研磨コレクションの詳細に出す
js/atelier-ui.js      研磨工房の画面（研磨ミニゲーム、自動研磨、コレクション）。ui.js と相互に import している
js/util.js            rand / chance / fmt などの小物
sw.js                 Service Worker（ネットワーク優先、オフライン時はキャッシュ）
manifest.webmanifest  PWA 設定
tools/icon.html       アプリアイコンの生成ページ
tools/artifact-preview.html  秘宝の絵の一覧プレビュー
tools/test/           Playwright のテストスクリプト（§7）
docs/GEMS.md          ジュエルパワーの対応表
```

---

## 4. ゲームの仕様（現在の値）

### ステージ（js/stages.js）

| # | id | 名前 | 時間 | 敵HP / 敵攻撃の倍率 | ギミック | 最終ボス | 初回クリアで解放 |
|---|---|---|---|---|---|---|---|
| 1 | wastes | 黒曜の荒野 | 7分 | 1.0 / 1.0 | なし | boss3 オブシディアン・クイーン | — |
| 2 | cavern | 水晶洞窟 | 7分 | 1.25 / 1.1 | 水晶柱（通れない・敵弾を遮る） | prism | トルマリン |
| 3 | magma | 灼熱鉱脈 | 7分 | 1.5 / 1.2 | 溶岩だまり・噴火 | worm（胴体つき。胴体への攻撃はダメージ×0.2で頭へ） | アレキサンドライト |
| 4 | tundra | 凍晶氷原 | 7分 | 1.85 / 1.3 | 吹雪（減速 0.72・視界不良・風） | lich | ムーンストーン |
| 5 | void | 虚空聖堂 | 8分 | 1.9 / 1.35 | 暗闇（見える範囲 175、フィーバー中 205）・虚空の裂け目 | emperor（3形態） | — |

- **1 戦の長さ**：ユーザーの指示で 10 分 → 7 分に縮めた。予定表（出現表・出来事）はもとの 10 分（虚空聖堂は 12 分）の秒数のまま stages.js に書いてあり、
  最後に TIME_SCALE（0.7）を掛けて縮めている。ボスの時刻とステージの長さは分単位に切り捨て（「2:06 はキモい」とユーザーに言われた）。ボスの予告はボスの 8 秒前。以下の本文の時刻は、特に断りがなければ**もとの 10 分基準**。
  難易度の曲線（hpScale・敵の攻撃力の伸び・噴火や裂け目の激しさ）は game.js の progress()（= 経過時間 ÷ 0.7）で計算し、7 分でもとの 10 分と同じ強さになる。
  レベルアップの必要経験値とフィーバーの必要量は一度 0.7 倍にしたが、「上がりすぎる」とのことで戻し、さらに必要経験値は 10 分のときの 1.25 倍にした（ユーザーの指示）。ユーザーレベルの経験値は生存秒 ÷ 4.2（1 戦あたりは以前と同じ）。
  実際の時刻：中ボス 2:00・4:00、最終ボス 7:00（虚空聖堂は 6:00 に中ボス、8:00 に最終ボス）。秘宝の宝箱は 2 体目以降の中ボスから（開始 3:30 以降）。
- 各ステージでは、3:00・6:00 などに中ボス、最後に最終ボスが出る（makeEvents）。最終ボスを倒すとクリア。
- **7:00 以降のイベント**（「7〜10 分にやることがない」という指摘への対策。雑魚は強くしない方針）：
  - ジュエルシーフ（ENEMIES.thief、AI は enemies.js の thief）：7:12・8:12・9:42 ごろに出る金色の亡霊。自機から逃げ回り（画面の端に沿って走る、ときどき立ち止まる）、15 秒で消える。
    接触ダメージなし。倒すと原石（30% で大原石、upgradeTier あり）・コイン・宝箱。画面外にいるときは THIEF の矢印を出す。
  - 大群ラッシュ：wall（一列の壁が画面を横切る）と vortex（出現時の自機の位置を中心に、渦を巻きながら縮む）。敵はステージの swarm と同じ弱い種類。
  - 12 分ステージ（虚空聖堂）は 10:00 以降にも追加。エンドレスの抽選にも入れている。
- **雑魚の硬さ**：時間による伸び（hpScale、10 分で約 14 倍）に加えて、自機の強さにも少しだけ連動する（game.js の estPower と trashMul）。
  estPower は武器ごとの DPS の目安（bench.js の実測：Lv1 約 300、Lv4 約 700、Lv8 約 6500、進化後 約 20000）の合計 × 攻撃力 ÷ クールダウン。
  trashMul = √(estPower ÷ 3500) で、1〜10 倍（2 武器 Lv8 で約 2.0、4 武器 Lv8 で約 3.0、4 武器進化で約 5.2）。
  ユーザーの意向は「完全に連動させると強くなった実感がないので、終盤の雑魚が序盤より少しだけ相対的に弱い」くらい。概算では 2 分時点と比べて終盤は 2〜3 倍柔らかい。
  最初は ÷2200・上限 12 で入れたが、ボットの生存時間が縮んだため緩めた（ただしボットは結果のばらつきが大きい）。
  大群イベント（swarm / wall / vortex、spawnEnemy の `soft`）・ジュエルシーフ・**エリート**は対象外。
  エリートは当初対象にしていたが、もとの「hpScale × 12」に掛かって 4 武器進化の終盤で 80〜90 万 HP になり、「全然削れない」と指摘されて外した。
- **ボス戦中の雑魚**：ボスが生きている間は、雑魚の同時出現数と出現ペースを 0.65 倍にする（director。エンドレスの値にも掛ける）。「絞りすぎるとヴァンサバらしくない」ので少しだけ。
- **敵の攻撃力**：雑魚・エリートは 2 倍、ボスは 1.5 倍（spawnEnemy の dmg。敵の弾・レーザーも e.dmg から計算するので同じ倍率がかかる）。ユーザーの指示で 2 倍にし、ボスは「2 倍だと火力がやばいかも」とのことで控えめにした。ステージのギミック（溶岩 12 ／ 0.4 秒ごと、噴火 32。× ステージの攻撃倍率）も 2 倍にした。
- **ヒート**：難易度 0〜5（HEAT_MAX）。1 段階ごとに 敵HP +25%、敵攻撃 +12%、敵速度 +5%、出現数 +10%、コイン +30%。
- エンドレスモードあり。
- **HYPER**（クリア済みステージで選べる）：自機・敵の移動速度 ×1.65、敵弾 ×1.2、獲得コイン ×1.5。**HURRY**：ステージの時計（`this.time`）が 2 倍速で進む（敵の波・イベントが半分の実時間で来る）。報酬の上乗せはなし。どちらもヴァンサバのハイパー／ハリーを参考にした。
- 過去のボット検証でのクリア率の目安：ステージ1は強化なしでもクリア可能、ステージ2〜4は工房強化ありで約5割、ステージ5は約2割。

### 武器（js/data.js の WEAPONS、挙動は js/weapons.js の LOGIC）

- 全 15 種。最大 Lv8（WEAPON_MAX）。Lv8 で対応するチャームを持っていると、レベルアップの選択肢に「進化」が出る。
- **ダメージの計算**：`dmg × 攻撃力倍率 × dmgMul ×（進化後は evo.mul、未進化なら低レベル補正）`
  - `dmgMul`：武器ごとの調整値。
  - `lowBoost`：低レベル時の補正。Lv1 で `1 + lowBoost` 倍になり、Lv8 で補正なしになる。現在はサファイア 0.7、ロードクロサイト（フレイム）2.5。
  - 調整の目標：t=300 秒時点のベンチマークで、Lv8 ≈ 6〜7k DPS、進化後 ≈ 20k DPS 前後に揃える（tools/test/bench.js）。
- フレイムは低レベル時に射程も補正している（Lv1 で +60%）。進行方向に炎を出すので、敵から逃げながら戦うと当たりにくいのが弱点。
- ロードクロサイト（フレイム）は **炎をすべて前方に出す**（ユーザーの指示。以前は Lv5 で後方にも、進化後は周囲を回転していたが「個性が失われる」とのこと）。
  弾数が増えると前方の扇が広がり（1 本ごとに 0.24 rad）、進化後は途切れず噴き出して射程 1.5 倍・扇 +1 本・炎の先に火だまりを残す。本数が多いほど 1 本ずつを薄く描く（白飛び防止、`fa`）。
  dmgMul 3.75・lowBoost 1.33（前方だけにしたときに 2.5・2.5 から変更。Lv1 の強さは同じ）。当てにくい代わりに正面の火力が高い、という位置づけ（ユーザーの意向）。
  bench.js のボットは逃げながら戦うので前方の武器を正しく測れない（Lv8 約 3k、進化後 約 14k と出るが、実際に突っ込むとこれより強い）。
- ガーネットは命中時に HP を吸収する。
- エンジェライトは装備中ずっと HP を回復する（毎秒 Lv1 0.75〜Lv8 1.8、進化後 3.0）。「回復しているか分からない」と言われて引き上げた。
- ムーンストーンは敵を「魅了」して同士討ちさせる（charmDmg）。進化後に周回する月は、丸い月だけを描く（菱形の光は付けない）。

| 武器 | 名前 | 進化の相手 | 進化後 |
|---|---|---|---|
| ruby | ブレイブ・バレット | jasper | ブレイブハート・バースト |
| sapphire | ボンド・オービット | nephrite | エターナル・ボンド |
| garnet | アムール・ハート | milkyquartz | アムール・テンペスト |
| labradorite | ヒドゥン・ボルト | iolite | ヒドゥン・ストーム |
| opal | ミラクル・プリズム | coral | ミラクル・スペクトラム |
| amber | ゴールド・レイン | titanite | ゴールデン・ジャックポット |
| angelite | セラフ・オーラ | redberyl | サンクチュアリ |
| diamond | ブリリアント・カット | topaz | スターダスト・カリスマ |
| emerald | ハーモニー・フィールド | granite | エデン・ガーデン |
| rhodochrosite | パッション・フレイム | peridot | パッション・インフェルノ |
| kyanite | ディサイシブ・ランス | prase | ディサイシブ・ブレード |
| aquamarine | タイダル・ウェーブ | coal | アビサル・タイド |
| alexandrite | ドリーム・シフト | turquoise | ポッシビリティ |
| tourmaline | ボンド・リコシェ | citrine | グランド・アドベンチャー |
| moonstone | ルナ・ハロ | milkyquartz | フルムーン・グレイス |

### チャーム（PASSIVES）

- 全 14 種：topaz, jasper, nephrite（最大Lv1）, milkyquartz, coral, iolite, prase, granite, coal, titanite, redberyl, turquoise, peridot, citrine。
- シトリン（citrine、ジュエルパワー「商売繁盛」）：敵がコインを落とす確率 +2%/Lv（stats.coinDrop。もとの 5% に足す）。獲得コインの量を増やすチタナイトと効果が重ならないように「落ちる数」にした。雷属性。トルマリンの進化の相手（以前はネフライトで、サファイアと重複していた）。
- 図鑑用のフレーバーテキストは GEMS の `lore` に入っている（武器の宝石と同じ書き方）。

### 経験値・レベル・装備枠

- **装備枠は武器 4・チャーム 6**（data.js の `MAX_WEAPONS` / `MAX_CHARMS`）。HUD には空き枠を点線で表示する（1 行目が武器、2 行目がチャーム）。
  - 経緯：6/6 → 4/4 → 武器 4・チャーム 6。武器もチャームも同じ数だと、全武器を進化させるだけでチャーム枠が埋まってしまうため。
- 必要経験値：`xpFor(l) = round(1.25 × 1.75 × 1.8 × (3 + (l-1)×4 + max(0,l-15)×4 + max(0,l-30)×6 + max(0,l-60)×10))`（game.js）。「途中で強化し終えて後半の楽しみがない」という要望を受けて、2 回に分けて引き上げた。
- レベルアップの選択肢には、図鑑に未登録かどうか・装備中の武器の進化素材かどうかのタグが付く。
- **リミットブレイク**（type 'lb'、data.js の LIMIT_BREAK）：Lv8 の武器のうち、進化済みの武器と、もう進化できない武器（進化用チャームがなく、チャーム枠が埋まっているか、そのチャームを BANISH した）に、小さな強化を 1 つ付ける選択肢が出る。
  以前は「進化用チャームを持っていない」だけで出していたため、まだ進化できるのにリミットブレイクばかり出るとユーザーに指摘された。
  ダメージ +6% ／ クールダウン短縮 3% ／ 範囲 +5% ／ 弾速 +6% ／ 持続 +6% ／ 弾数 +1（最大 2 回）。武器が持っている性能だけが候補になる。
  強化は `w.lb`、回数は `w.lbN` に持ち、weaponStats で掛ける。HUD では「★+3」のように回数を出す。ユーザーの指示で「微強化」にしている。
- レベルアップ画面の操作：REROLL（選び直し）、SKIP（何も取らずに閉じる）、BANISH（押してからカードをタップすると、その武器・チャームをこのランの候補から外す。進化は外せない）。回数の初期値はリロール 2・スキップ 1・バニッシュ 1 で、工房で増やせる。
- レベルアップの選択肢には、通常の 3 枠（幸運が高いと 4 枠）に加えて、**「HP 25% 回復」が常に 1 枠**付く（type 'heal25'、細いカードで表示）。選択肢が 5 つになったときは詰めたレイアウト（.lvl-screen.many）になる。
- 回復した量は、0.6 秒ごとにまとめて**緑の数字**で表示する（被ダメージは赤の数字）。game.js の heal() が healShow に加算している。

### 属性（js/elements.js）

- 8 属性：炎・水・氷・草・雷・光・闇・土。全 29 種の宝石（武器もチャームも）に 1 つずつ付けている（GEM_ELEMENT）。
  - 炎：ルビー、ロードクロサイト、コール、レッドベリル
  - 水：アクアマリン、サンゴ、アイオライト
  - 氷：サファイア、カイヤナイト、ミルキークオーツ
  - 草：エメラルド、ネフライト、クリソプレーズ、ペリドット
  - 雷：ラブラドライト、トルマリン、コハク、シトリン
  - 光：オパール、エンジェライト、ダイヤモンド、トパーズ、チタナイト
  - 闇：ムーンストーン、ガーネット、アレキサンドライト
  - 土：グラナイト、ジャスパー、ターコイズ（攻撃石なし）
  - 割り当ての方針は、武器の挙動・色・実物の特徴（コハク＝electricity の語源、アイオライト＝ウォーターサファイア など）。進化の組み合わせで属性が違ってもよい（ユーザーの判断）。
- **ユーザーの意向は「あくまでオマケ要素」**。効果量も発動率も控えめにする。4 つ揃えられない属性があってもよい。
- 発動：攻撃が当たるたびに、その武器の属性を 4% で抽選する。**武器ごとに抽選のクールタイム 0.2 秒**（何度も当たる武器ほど発動しやすくなるのを防ぐ。係数方式ではなくクールタイム方式はユーザーの指定）。
  実測では、どの武器もおよそ 6 秒に 1 回発動する。継続ダメージや連鎖などの追加ダメージ（`o.dot`）では抽選しない（クリティカル・ルーペも出ない）。
- 揃えたボーナス：同じ属性の宝石の数（武器もチャームも 1 と数える）が 2 で 1.15 倍、3 で 1.3 倍、4 以上で 1.5 倍。効果量と効果時間に掛ける（発動率には掛けない）。
- 効果（状態異常／その状態の敵への追加効果）：炎＝燃焼（3 秒で与ダメの 24%）／与ダメ +8%、水＝濡れ（移動 -25%、ボス -10%）／接触ダメージ -15%、
  氷＝凍結 1 秒（ボスは減速）／クリティカル率 +10%、草＝寄生 4 秒（与ダメの 1.5% で回復）／撃破で HP +1、雷＝感電（与ダメの 20% が近くの 1 体へ）／感電中なら 2 体、
  光＝目くらまし（敵の AI の時間を 1/1.3 に＝攻撃の間隔が延びる）／撃破で経験値 +10%、闇＝恐怖 1.5 秒（逃げる。ボス・シーフ無効）／撃破で近くの敵にうつる、
  土＝自己バフ（6 秒ごとに 40% で 5 秒間、被ダメージ -15%・ボスのブレイクゲージ +15%。土の宝石を持っているときだけ）。
- 上限：草の回復は毎秒 1（×揃えた倍率）まで（与ダメ比例なので、終盤の火力だと上限がないと回復しすぎる）。減速・軽減の量にも上限を付けている。
- 秘宝「氷晶のルーペ」は属性と別にそのまま（ユーザーの指示）。
- 表示：ジュエルパワーの札の横に属性の札（elemTag。ジュエル選択・図鑑・研磨）、レベルアップのカードの LV の横、ポーズ画面に揃えた数と倍率、遊び方（HOW TO PLAY）の「属性」に一覧（以前は図鑑に「属性」タブがあったが、ユーザーの判断で遊び方に移した）。
  状態異常の敵には属性の色の細い輪を 1 つだけ描く。土の加護中は自機の周りに点線の輪。

### フィーバー

- 撃破でゲージがたまり（雑魚 1、エリート 15、ボス 60）、必要量は最初 120、発動するたびに ×1.35。
- 10 秒間、与えるダメージ ×1.5、経験値 ×2、画面の縁が細い虹色になる。
- 回収範囲は 10 秒かけて外側へ広がり、最終的に盤面全体の経験値を回収する。以前は発動した瞬間に全部を吸い寄せていたため、数レベルが一気に上がる不具合があった。**一括で回収する方式には戻さないこと**。

### ボス

- **移動（game.js の bossMove）**：常に追いかけてはこない。`far = 170 + r` より離れると急いで接近（速度×1.9）し、それより近いと一定距離を保って周回する（速度×0.6、4〜8 秒で回る向きが変わる）。突進技はこれまでどおり。worm は除外。画面外に出るとワープで戻る。
- **撃破時**：画面上の敵の弾をすべて消し、レーザーも消す。宝箱、経験値、コインを落とす。
- **ブレイク**（game.js の addBreak / bossBreak）：ボスに与えたダメージでブレイクゲージがたまる。ボスの体の縁からの距離が 70 以内で 2 倍、320 以上で 0.4 倍の速さ（その間は直線）。
  満タン（最大 HP の 34% ×（1.4 の累乗、ブレイクのたびに増える）÷ 距離倍率ぶんのダメージ）で 4 秒ダウン：動かない・攻撃しない・接触ダメージなし・被ダメージ 2 倍。
  ダウンした瞬間に、そのボスのレーザーと予告円を消す。HUD のボス HP バーの下に細いゲージ（近くで戦っていると明るく、ダウン中は点滅）、ボスの頭上に回る星。
  目的は「弾幕と体当たりを避けてじっと耐えるだけ」のボス戦に、近づいて攻める理由を作ること（ユーザーの要望）。ボスの HP は据え置き。
  最初は 24%・1.3 倍で入れたが、「もう少し頻度を下げていい」とのことで 34%・1.4 倍に（1 戦で 2〜3 回ほど）。

### メタ要素

- **裏工房（BACKROOM）**：工房の強化をすべて最大にすると解放（`save.backShop`。一度解放したら、工房を返金しても閉じない）。工房画面の上にボタンがある。
  内容と強化回数は工房と同じで、効果は半分・費用は 12 倍。回数を増やす強化（リロール・スキップ・バニッシュ）とリバイブ・弾数は、効果はそのままで費用 24 倍（data.js の backShopRate）。
  レベルは `save.upgrades2`、無効化は `save.upgrades2Off`。返金は工房と別。画面の見出しは「BACKROOM ／ 裏工房」だけ（効果や費用の説明文を見出しに書かないこと。ユーザーに指摘された）。
- **工房（SHOP）**：永続強化。最大まで上げた強化は ON／OFF を切り替えられる（`save.upgradesOff`）。「全額返金」で全強化を Lv0 に戻し、使ったコインを全額返す。画面には「攻撃力 +15% → +20%」のように今の値と次のレベルの値を表示する。表記は「クールダウン短縮 X%」（「クールダウン -X%」はデメリットに見えると指摘された）。
- **採掘（旧・原石ガチャ）**：画面上の名前は「MINING ／ 採掘」（ユーザーの指示で改名。コード上の id は gacha のまま）。1回 300、10連 2700（10連は大原石以上が 1 枠確定）。中身は原石で、UR 3% 秘石 ／ SSR 10% 大原石 ／ SR 27% 原石 ／ R 60% 原石の欠片×2（data.js の GACHA_TABLE）。引くたびにジェムダスト（UR 10 ／ SSR 5 ／ SR 2 ／ R 1、`save.dust`）が貯まる。
  - 交換所（ガチャ画面の EXCHANGE）：秘石 40、大原石 15、覚醒 30（解放済みジュエル、攻撃力 +5%、最大 5 段階）。覚醒は交換所でしか手に入らない。
  - 経緯：ガチャの中身がキャラ解放と覚醒だけで、工房・研磨に比べてコインを入れる理由が弱かったため、「キャラは遊んで解放」「ガチャは原石の仕入れ」「ハズレはダストにして交換」に作り直した。
  - コイン報酬のアイコンは coinIcon（金貨 3 枚重ね）。琥珀のアイコンを使わないこと。
- **キャラ（CHARACTERS）**：最初から使えるのは ruby / sapphire / garnet。そのほかは実績またはステージクリアで解放する（ガチャからは出ない）。実績側の `unlock` で指定：labradorite（1000体撃破）、amber（コイン500枚）、angelite（5分生存）、diamond（荒野クリア）、emerald（Lv30）、kyanite（1プレイでボス2体）、aquamarine（HEAT1以上でクリア）、rhodochrosite（1プレイでフィーバー5回）、opal（研磨で14種コレクション）。ステージ：tourmaline / alexandrite / moonstone。起動時に、実績を持っているのに未解放のキャラは解放する（main.js）。
- **図鑑**：武器・チャーム・敵・秘宝・ステージ・実績。
- **ログインボーナス**：7 日周期。
- **セーブ**：`save.stages[id] = { cleared, heat, best }` など。古いセーブの移行処理は main.js にある。
- **秘宝（ARTIFACT）**：ヴァンサバのアルカナにあたる、宝石の道具（js/artifacts.js）。全 11 種。
  - 入手：ステージ選択で 1 つ持ち込める（`save.artSel`）。6:00 以降の中ボス（最終ボス以外）を倒すと「秘宝の宝箱」が開き、未所持から 3 つ提示。どちらも「選ばない」を選べる。1 ランで最大 3 つ（ARTIFACT_MAX）。
  - 解放は実績の達成（各秘宝の `ach`）。解放済みかは save.achievements から判定するので、別途の保存はない。
  - 効果：ロードストーン（偶数分ごとに全回収）／護石のペンダント（復活 +1、復活すると最大HP +20%・アーマー +1・攻撃力／範囲／弾速／持続 +10%）／癒しの聖杯（回復 2 倍＋衝撃波）／空の宝石箱（空き武器枠 1 つにつき攻撃力 +20%・CD 短縮 8%、復活 +1）／研磨ホイール（移動中は武器の動作 1.5 倍速）／氷晶のルーペ（命中時 8% で凍結）／黄金の天秤（ゴールドフィーバー）／職人の王冠（初期武器の弾数 +3）／呪われた宝石（1 分ごとにエリート）／流星のオルゴール（レベルアップ時にアイテムが降る）／分光プリズム（攻撃範囲が 10 秒周期で ×0.5〜×3.0）。
  - 絵は宝石の流用ではなく、道具ごとに描き下ろし（render.js の artifactIcon → artifact-art.js）。ユーザーの指摘（ルーペの持ち手が離れていた）のあと、全種を拡大して見直し、ペンダントの鎖・磁力線・聖杯の湯気・研磨ホイールの火花・天秤の金貨も直した。
  - 調整の経緯：ペンダントと宝石箱は当初「復活 +3」だったが、倒れたら終わりのゲームでは重すぎる（ユーザー判断）ため +1 に下げ、ペンダントの強化幅を 2 倍にした。宝石箱の解放条件は「武器1つでクリア」→「武器2つ以下でクリア」（宝石箱の効果があって初めて成り立つ条件を解放条件にしていたため）。
  - 選別の経緯：ユーザー提示の候補 21 個から、「一部武器」を名指しする仕様と重複系統を除いて選んだ。弾速・効果時間の周期変動はプリズムに統合。「経験値ジェムが爆弾になる」はやり込み用の縛りとして将来の候補。
- **研磨工房（ATELIER、タイトル画面から）**：ハムスター要素・コレクション要素として追加した。
  - 原石はラン中に手に入る：エリート撃破で欠片 20%／原石 5%、ボス撃破で原石か大原石、最終ボスは大原石か秘石（ヒートが高いほど秘石）。ステージとヒートに応じて、1 段階上位になる確率がある（upgradeTier）。クリア時に拾い損ねた原石は自動で回収する。
  - 等級は 欠片 / 原石 / 大原石 / 秘石。研磨にはコインが必要（30 / 80 / 200 / 500）で、コインの使い道も兼ねている。上位ほど大きなカラットと、レアな宝石が出やすい。
  - 研磨ミニゲーム：リングを回る針が光る面に重なったらタップする。面の数は等級ごとに 3〜6。判定の幅は PERFECT ±0.2 / GREAT ±0.4 / GOOD ±0.64 rad（× 等級ごとの zone 係数）で、得点は PERFECT 1 / GREAT 0.7 / GOOD 0.4 / MISS 0。平均が 0.95 以上で SS、0.8 以上で S、0.6 以上で A、0.35 以上で B、それ未満は C。研磨画面に入ったら、最初のタップで開始し、「READY」の間を置いてから針が動き出す。中に入っている宝石は研磨の前に決まっていて、研磨中の光の色がヒントになる。
  - 研磨で出る宝石：実際の産出量をおおまかに 5 段階にした重み（atelier.js の ABUNDANCE。石英・花崗岩・石炭・碧玉が最多、アレキサンドライトとレッドベリルが最少）。
    ユーザーの指示は「現実の産出量に準拠。ただし厳密だと偏りすぎるので緩く」。重みの差は欠片で最大 10 倍、上位の原石ほど差が縮む（秘石で約 2.5 倍）。
  - 品質の色：SS 虹色 ／ S 金 ／ A 紫 ／ B 青 ／ C 緑（ユーザーの指定。atelier.js の GRADES）。
    レア度の色も同じ並びにそろえる：UR 虹 ／ SSR 金 ／ SR 紫 ／ R 青 ／ N 緑（style.css の .r-*、ui.js の RANK_COLOR）。採掘の結果に使っている。
    ただしラン中のレベルアップのカードだけは N を白にする（.lvl-screen .r-N。全部に色が付くと SR 以上が目立たないため。ユーザーの指示）。
  - 自動研磨：その等級の原石をまとめて磨く。品質は C か B。
  - コレクション：全 29 種（武器の宝石 15＋チャームの宝石 14）。宝石ごとに `save.jewels[id] = { n: 研磨数, best: 最高品質(0〜4), ct: 最大カラット }` を記録する。
  - コレクションのマスは宝石名が見出しで、右上に品質の記号とその下に練度の数字だけ（「練度」の文字は付けない。ユーザーの指示）。タップすると詳細が開き、下部の GEM DATA に実在の宝石の情報（js/gem-facts.js）を載せる。並びはモース硬度・主な産地・名前の由来、最後の枠に「どんな石か（和名から始める一文。例：紅玉。コランダム〜）」と小ネタを段落を分けて入れる（「そもそも何の石か分からない」と言われて追加）。1 回も研磨していない宝石は ？？？ で伏せる。
    情報はゲームの設定と切り離して、実際の宝石学の内容だけを書く（図鑑ではなく研磨コレクションに置く、はユーザーの判断）。
  - 練度 = 最高品質（C=1〜SS=5）＋研磨数 3/10/30 でそれぞれ +1（最大 8）。練度に応じて、宝石ごとのジュエルパワーに沿った小さな永続ボーナスが付く（atelier.js の MASTERY_BONUS。computeStats で加算）。
    ボーナスには最大カラットの倍率 `1 + ct / 10` を掛ける（3.69ct なら ×1.369。atelier.js の caratMul）。工房に比べて弱いと言われて追加した。
  - 実績（meta: true）：初研磨、100 個研磨、SS、14 種、28 種、練度最大。meta の実績はラン以外（研磨時）にも判定する（main.js の checkAchievements(null, true)）。
- タイトル画面のメニューは START、WORKSHOP、ATELIER（未研磨の原石数をバッジ表示）、MINING（採掘）、ARCHIVE（図鑑・実績）、一番下に細い HOW TO PLAY（遊び方）。
- **遊び方（HOW TO PLAY）**：説明と仕様をすべてここにまとめる（js/howto.js、項目ごとに開閉）。各画面にある説明は、その場の方が分かりやすいので消さない（ユーザーの判断）。
  仕様や数値を変えたら、js/howto.js も合わせて直すこと。以前の RECORDS ボタンは、図鑑の「実績」タブに統合した。
- **ユーザーレベル（ランク、js/rank.js）**：ランが終わるたびに経験値が入る（settleRun。ボットでは入らない）。
  経験値 = (生存秒 ÷ 6 ＋ 撃破数 ÷ 20 ＋ ボス × 20 ＋ クリア 100) × (1 ＋ 0.1 × ヒート)。10 分のクリアで 400 前後。次のレベルまで 80 ＋ 25 × Lv。
  恩恵は獲得コイン：1 レベルにつき +0.5%（Lv50 で 1.25 倍。computeStats で greed に掛ける）。
  称号：Lv1〜19 アクリル、20〜49 グラス、50〜 クリスタル（「級」は付けない。区分の一覧は画面に出さない。どちらもユーザーの指示）。
  タイトル画面の左上に RANK の窓（下端に経験値のバー）。タップで、称号・次のレベルまでの経験値・今と次の効果量のカード。リザルト画面のコインの下に経験値のバー（レベルが上がると RANK UP）。
- **バックアップ**（設定画面）：書き出すと JSON ファイルを共有メニューに渡す（iPhone では「"ファイル"に保存」で iCloud Drive へ）。読み込むときは内容を確認してから上書きする。`save.lastBackup` に最後に書き出した時刻を記録している。

---

### 実装上の注意（バグ精査で直した点）

- 空間グリッド（game.js の Grid）は、半径 24 を超える敵（ボスなど）をセルに入れず `big` に別で持ち、query で必ず距離判定する。中心のセルだけで判定すると、大きいボスの縁に範囲攻撃が当たらなかった。
- メインループは次のフレームを先に予約し、`step()` を try/catch で包んでいる（1 回の例外でゲームが固まらないように）。
- 最終ボス撃破からクリア画面までの間は被弾しない。finishRun は `res.cleared` も見てクリア扱いにする。
- ボス撃破時は、そのボスの予告円（warn の owner）も消える。別のボスが生きていれば、HP バーと BGM はそちらに引き継ぐ。
- 時間停止中はレーザーと予告円も止まる。皇帝は形態変化を飛ばして HP が減らないよう、次の形態の境目で HP を止める。
- エリートのボマーは自爆しない。宝箱と原石は水晶柱から押し出す。遠く（viewR×3 以上）に取り残したコインなどは消す。
- 研磨のコインは、研磨を終えたとき（原石の消費と同時）に払う。品質判定は浮動小数点の誤差を 1e-9 だけ許容する。
- バックアップを読み込んでも、ログインボーナスの受け取り状況は今のものを引き継ぐ。
- **途中保存**：ラン中は 5 秒ごと・アプリが裏に回ったとき・ページを離れるときに、`game.results()` を `save.pendingRun` に書く（main.js の saveRunSnapshot）。iPhone がアプリを終了させた場合は、次の起動時にリタイア扱い（最終ボス撃破後ならクリア扱い）で報酬を反映し、「RECOVERED」の通知を出す（recoverPendingRun）。通常の終了では settleRun が pendingRun を消す。ボットでは書かない。
- ハートの回復も heal() を通す（聖杯の 2 倍・緑の数字・回復量の集計が効くように）。

### 新しい宝石を追加するときの手順（更新する場所）

必ず更新する：
1. js/data.js の `GEMS`：jp・en・word（ジュエルパワー）・kana・color / light / dark・lore（図鑑のフレーバー）。絵のカットは js/gem-art.js の `GEM_CUT`（brilliant / oval / cushion / pear / trillion / step / baguette / cab / cabOval / crystal / tumble / lump）
2. チャームなら js/data.js の `PASSIVES`（per・max・t）。武器なら `WEAPONS` と js/weapons.js の LOGIC（挙動と描画）、`CHARACTERS`（特性と解放条件）
3. js/elements.js の `GEM_ELEMENT`（属性）
4. js/atelier.js の `MASTERY_BONUS`（研磨の練度ボーナス）と `ABUNDANCE`（研磨で出る重み 1〜5）
5. js/gem-facts.js（実在の宝石の情報：d＝どんな石かの一文紹介、h＝硬度、o＝産地、n＝名前の由来、t＝小ネタ。サブエージェントに調べさせ、長ければ要約）
6. docs/GEMS.md の表、README の種類数、CLAUDE.md（チャーム・武器の一覧、属性の一覧、コレクションの種類数）
7. sw.js の版数
効果が新しい種類なら：data.js の `BASE_STATS` に初期値、効果の処理（game.js など）、atelier.js の `STAT_LABEL`（研磨ボーナスの表示名）。
必要なら：武器の進化の相手（`WEAPONS` の `evo.with`。進化の相手は気軽に変えてよい、とユーザー）、秘宝・工房のアイコンの宝石。
自動で反映される：レベルアップの候補、図鑑、研磨コレクションのマス、宝石の絵、タイトルの背景、遊び方の属性の一覧、コレクションの総数（実績「ジュエル・マスター」と遊び方は GEMS の数から出す）。

## 5. 画面・演出の決まりごと

- タイトル画面のロゴの下には何も置かない。画面の一番下のクレジット表記もなし。
- ジュエルのレア度（R / SR / UR）は画面に出さない（「基準もシステム的な意味もない」とユーザーに言われて外した）。CHARACTERS の `rarity` も削除した。
- **宝石の絵**（js/gem-art.js の drawCutGem）：実物に近いカット別のテンプレートに、GEMS の色（color / light / dark）を乗せて描く。
  ブリリアント系（ラウンド・オーバル・クッション・ペア・トリリアント）、ステップカット系（エメラルドカット・バゲット）、
  カボション（不透明な石。オパールの遊色、ラブラドライトの光の帯、ムーンストーンの揺らめき、ターコイズの網目、ロードクロサイトの平行な縞など）、原石風（乳石英の結晶・花崗岩の丸石・石炭の塊）。
  ユーザーの判断：ハートなどの変わった形はやめて一般的なカットに、不透明な石はカボションに。ロードクロサイトの同心の縞は「キモい」と言われて平行な縞にした。
  gemSprite / gemIcon を宝石の id で呼ぶと新しい絵になる。形を指定した呼び出し（ガーネットの弾のハート、カイヤナイトの槍、トルマリンの光など）と、GEMS 以外の色の宝石（経験値の宝石など）は従来の drawGem のまま。
- 採掘画面の中央はピッケルの絵（render.js の pickaxeIcon）。最初は白く光り、溜めの間は振り下ろす動きをして、最高レアの色に光る。
- ジュエル選択の詳細欄は「名前（1 行目・折り返さない）／英名＋ジュエルパワー（2 行目）／武器と特性」の固定レイアウト。
- カードの背景は `var(--panel)`（半透明の黒）にして、背景が透けすぎないようにする。ボタン（.btn）やタブも同じ黒地を敷いている。
  押せない・未解放の状態を `opacity` で表すと背景まで透けてタイトルの宝石が文字に重なるので、`filter: brightness()` で暗くする。
- **文字の折り返し**：名前・見出し・短い説明文には `text-wrap: balance` を指定している。`text-wrap: pretty` は**全体にかけない**
  （iPhone の Safari の pretty は段落全体の行をそろえ直すので、長い説明文が balance と同じ不自然な切れ方になる。Chromium の pretty は最後の数行しか直さないので、テスト環境では気づけなかった）。名前は `word-break: keep-all` にして、
  ui.js の addWbr（MutationObserver）が「・」の直後に `<wbr>` を入れるので、「ゴールデン・／ジャックポット」のように「・」の位置で分かれる。
  画面や文言を足したら tools/test/wrap.js で「最後の行が 1〜2 文字だけ」の箇所がないか確認する（375 / 390 / 430 幅）。
  `balance`（上下の行の幅をそろえる）は**短い文字だけ**（名前・見出し・バナー・カードの説明・注記）。図鑑やジュエル選択の長い説明文にかけると
  「最も HP ／ の高い敵へ」のように不自然な位置で切れるので、ふつうに折り返す（ユーザーの判断：長い説明文の改行は仕方ない、カットインなどの短い文字の改行がダサいのが嫌）。
  wrap.js も .ztext / .zevo / .lore / 研磨コレクションの詳細の説明文はチェックしない。
  wrap.js はボタンの文字の見切れ（CLIP）も調べる。画面は 375×667（SE）・375×812・390×844・430×932。
  高さの上限があってスクロールする縦並びのパネル（.gem-detail / .exch / .at-sum）は、子を `flex-shrink: 0` にする（縮むと「閉じる」ボタンが押しつぶされて文字が見切れた）。図鑑の「（Lv8 ＋ 宝石名）」は `.nw` でひとかたまりにして折り返す。
- 大きい音（爆発 bomb・MIRACLE）は audio.js の voice('loud', 3, …) で重なりを抑える：同時に 3 つまで、重なっている数に応じて新しい音を小さくし、同じ音は 70〜90ms 空ける。
  アイテムのボムだけは bomb(true) で必ず鳴らす（マグマで噴火・自爆と MIRACLE が重なってうるさいと言われた）。
- リザルト画面はぼかし（backdrop-filter）を使わず濃い黒の背景にし、回る光（.rays）を fixed で小さくした。
  iPhone で、スクロールした先のダメージ内訳のパネルが途中から描画されずに抜けたため（Chromium では再現しない）。
- 実績の通知（toast）は小さめ。ラベルと名前を 1 行に並べ、同時に出すのは 3 件まで。
- 描画の順番（game.js の render）：地面・範囲攻撃 → アイテム → 武器の弾・炎・パーティクル → **敵 → 自機** → レーザー → **敵弾** → ダメージ数字。
  敵と敵弾は攻撃エフェクトより上に描く（分光プリズム＋進化フレイムで敵が見えなくなったため）。
- 敵のスプライトには黒（外側）と白（内側）の縁取りを付けている（render.js の outline）。敵弾も 黒の外縁 → 白の縁 → 色の芯 で描く。
- HUD の武器枠：進化用のチャームを入手済みの武器は金色の枠（.slotico.evok）、進化済みは白い枠と ★。
- 軽量化：ポーズ・レベルアップ・宝箱などの画面の間は、盤面を 1 回だけ描いて使い回す（上に重なる画面のぼかしを毎フレーム計算し直さないように）。
  ぼかし（backdrop-filter）は画面全体の .screen.dim だけに使い、ボタン・パネルには使わない。
- フレイムは加算合成の柔らかい光を進行方向に引き伸ばして描き、白飛びさせない。
- iOS 向けの工夫：safe-area への対応、touchend で音声を有効化、navigator.audioSession、iOS 18 の `<input switch>` を使った振動。

---

## 6. リリース手順（毎回これで行う）

1. 作業ブランチは `claude/ios-gem-vampire-survivor-zeod5k`。セッションの指示で別のブランチ名が指定されていれば、そちらに従う。
2. **sw.js の `CACHE` の版数を必ず 1 つ上げる**（現在は `jewel-survivor-v55`）。新しいファイルを追加したときは `ASSETS` にも足す。
3. `node --check` で構文を確認し、必要に応じて tools/test で動作を確認する。
4. main に取り込む手順（これまでは毎回 squash マージで、ブランチをリセットしてから載せ直している）：
   ```sh
   git fetch origin main && git stash && git checkout -B <branch> origin/main && git stash pop
   git add -A && git commit    # コミットメッセージは日本語で、何をなぜ変えたかを書く
   git push -f -u origin <branch>
   ```
5. GitHub MCP で PR を作り、squash マージする。main への push で `.github/workflows/pages.yml` が動き、GitHub Pages にデプロイされる（20 秒〜2 分程度）。
6. `actions_list` の `list_workflow_runs` で、実行結果が success になったのを確認してからユーザーに報告する。

### 更新の反映について

- GitHub Pages はファイルを 10 分キャッシュする。sw.js では同じサイトのファイルを `cache: 'no-cache'` で取得してこれを回避している（ページ遷移のリクエストは redirect: manual）。
- SW は `updateViaCache: 'none'` で登録し、アプリに戻ってきたとき（visibilitychange）にも更新を確認する。新しい SW に切り替わったら自動で再読み込みする。プレイ中・リザルト画面のときは中断せず、タイトルに戻ったときに再読み込みする（main.js の reloadPending）。
- アプリアイコンを変えたときは `?v=` の番号を上げる（index.html と manifest）。iPhone のホーム画面のアイコンは、ホーム画面に追加し直さないと更新されない（その際セーブが消える点も必ず伝える）。

---

## 7. 開発とテスト

### ローカル実行

```sh
npx http-server -p 8123 -s -c-1 .
```

### デバッグ用 URL パラメータ（main.js の DEBUG）

| パラメータ | 内容 |
|---|---|
| `auto=ruby` | タイトルを飛ばして即開始（ジュエルの id） |
| `bot` | 自動プレイ。結果は `window.__lastResult` に入る |
| `speed=8` | 1 フレームあたりのシミュレーション回数（早送り） |
| `norender` | 描画しない（高速化） |
| `t=300` | 開始時刻（秒）。その時刻より前のボスイベントは発生しない |
| `god` | 無敵 |
| `build=ruby,amber:evo` | 指定した武器を Lv8（対応チャーム付き）で持たせる。`:evo` で進化済み、`all` で先頭から装備枠いっぱいまで |
| `stage=cavern` / `heat=3` / `endless` / `hyper` / `hurry` | ステージ・ヒート・各モードの指定 |
| `art=grail` | 秘宝を持った状態で開始 |
| `cam=0.8` | カメラの倍率（小さいほど引いて広く見える）。試し用 |

`window.__game` からゲームの状態を直接触れる（例：`g.addWeapon(id)`、`g.modalQueue.push({type:'level'})`、`g.startFever()`、`g.rollChoices = () => [...]`）。

### テストスクリプト（tools/test/、Playwright を使用）

この環境では `NODE_PATH=/opt/node22/lib/node_modules` を付けて実行する（Chromium はインストール済み）。接続先は環境変数 `BASE`（既定は `http://localhost:8123`）。

```sh
# 自動プレイでバランスを確認（キャラ、速度、実時間の上限（秒）、追加パラメータ、タグ）
NODE_PATH=/opt/node22/lib/node_modules node tools/test/bot.js ruby 6 240 "" tag
# 武器の DPS ベンチマーク（開始時刻、計測秒数、武器リスト）。LV=1 で低レベル時を計測
NODE_PATH=/opt/node22/lib/node_modules node tools/test/bench.js 300 60 ruby,sapphire
LV=1 NODE_PATH=/opt/node22/lib/node_modules node tools/test/bench.js 60 40 ruby
# 全ステージの最終ボス撃破からリザルト画面までにエラーが出ないか
NODE_PATH=/opt/node22/lib/node_modules node tools/test/clear.js
# 文字の折り返しチェック（全画面・全カード・進化演出・実績通知を 3 つの画面幅で開き、最後の行が 1〜2 文字の箇所を一覧にする）
NODE_PATH=/opt/node22/lib/node_modules node tools/test/wrap.js
```

- ボットは敵から逃げ続ける動きをするので、実際の人の遊び方とは結果がずれる（経験値を拾いに行かない、フレイムが当たりにくい、など）。数値は目安として扱う。
- 見た目を変えたときは、Playwright で iPhone 13 相当の画面のスクリーンショットを撮り、Read で目視確認してから報告する。
- **テストの時間に注意**（ユーザーに「毎回長すぎる」と言われた）：wrap.js は文字や画面を変えたときだけ、`SIZES=375x667` で SE の画面だけ流す（全サイズだと 5〜10 分かかる）。
  clear.js（全ステージのクリア）も時間がかかるので、ステージやボスまわりを変えたときだけ。ふだんは構文チェックと、変えた箇所だけの短い確認で済ませる。
- 新規セーブではログインボーナスの画面が最初に出る。`#lok`（CLAIM）を押してから操作すること。

---

## 8. これまでの経緯（要約）

1. ヴァンサバライクを作成。当初は「ドーパミン全開」の方向性。
2. 黒ベースのスタイリッシュなデザインと大人向けの表記に全面変更。自機を「輪郭と瞳だけ」に。
3. GitHub Pages で公開（Source は GitHub Actions）。
4. 攻撃エフェクトの密度と視認性を調整し、武器ごとの DPS を揃えた。
5. 大型アップデート：5 ステージ、ギミック、新しい敵とボス、武器 3 種（アレキサンドライト・トルマリン・ムーンストーン）とチャーム 1 種（ターコイズ）、ヒート、ステージ選択。
6. 細かな要望への対応：タイトルの文言とクレジットを削除、「ジュエルパワー」に統一、レベルアップカードのタグ、フィーバーの縁取りを細く、フレイムを濃く、フィーバー中の経験値回収を段階的に、自機を黒い体に、必要経験値を約3.15倍に、低レベルのフレイムとサファイアを強化、ボスの移動を変更、ボス撃破時の弾消し、ムーンストーン進化の見た目、チャームのフレーバーテキスト、クールダウンの表記、ジュエル選択画面のレイアウト、工房の効果量表示、装備枠を 4 つに、コインのアイコン、更新が反映されない問題の修正、アイコンの作り直し、バックアップ機能。
7. 雑談で改善点を洗い出したあと、装備枠を武器 4・チャーム 6 に変更し、研磨工房（原石・研磨・コレクション）を追加。
   続けて、サブエージェント 3 体でバグ精査して一括修正。ガチャを原石ガチャ＋交換所に作り直し、ガチャ限定キャラを実績解放に移した。
8. ヴァンサバ本家の要素を調べて比較し、スキップ・バニッシュ、HYPER／HURRY、工房の返金・無効化を追加。
   続けて、アルカナにあたる「秘宝」11 種を追加（候補 21 個から選別）。絵を描き下ろし、ペンダントと宝石箱の強さを調整。
9. ステージクリア時にアプリが落ちたような挙動の報告があった。コード上はエラーが再現せず、端末側の問題と判断している。
10. 新しいセッションで全体を見直し、文字の折り返し（wrap.js で自動チェック）、カードの黒地の統一、実績通知の縮小、ハートの回復の不具合、途中保存を対応。
    終盤のエフェクトの多さ・ダメージ数字の多さも指摘したが、派手さとのトレードオフなのでユーザーの判断でそのままにしている。
11. 敵・敵弾を攻撃エフェクトより上に描き、黒と白の縁取りを追加。HUD に進化用チャームの入手済み表示。iPhone SE2 で落ちたとの報告を受けて軽量化
    （モーダル中の再描画停止、ボタン・パネルのぼかし削除）。メモリ使用量は Canvas 約 2MB・JS ヒープ約 10MB で、原因は特定できていない。
12. 7〜10 分の中だるみ対策として、リミットブレイク・ジュエルシーフ・大群ラッシュ（壁・渦）を追加（ユーザーが A+B+C を選択）。FEVER バナーの説明文がはみ出していたのも修正。
13. 「雑魚は柔らかすぎ、ボスは硬すぎ、ボス戦は耐えるだけ」という指摘を受けて、雑魚の硬さを自機の強さに少しだけ連動させ、ボスにブレイクゲージ（近いほどたまりやすい）を追加。
14. 実機での感想を受けて調整：オパールの MIRACLE の効果音は同時 6 個まで（audio.js の miracle）、エリートを硬さの連動から除外、ボス戦中の雑魚の出現を 0.65 倍、ブレイクの頻度を下げた。
15. プレーズを「クリソプレーズ」に改名。フレイムを前方だけで強くなる形に変更。裏工房を追加。研磨コレクションのボーナスに最大カラットの倍率。コインの表示が 6〜7 桁ではみ出さないように。
16. 図鑑などの長い説明文から balance を外した（不自然な位置で切れていた）。iPhone ではまだそろった見た目のままだったので、全体の pretty も外した。
17. 研磨コレクションのマスを宝石名の見出しに変更（練度は品質の記号の下）。詳細に実在の宝石の情報（GEM DATA）を追加。情報収集はサブエージェントに任せ、小ネタは短く要約した。
18. ジュエルのレア度表示を削除。採掘画面の紫のひし形をピッケルの絵に。
19. 研磨で出る宝石を実際の産出量に緩く準拠。品質の色を指定どおりに（A 紫・C 緑）し、レア度の色もそれにそろえた。「閉じる」ボタンの見切れを直し、wrap.js に見切れの検出を追加。
20. 属性システム（8 属性・オマケ程度）を追加。リミットブレイクが進化前に出る不具合を修正。
21. ユーザーレベル（ランク）を追加。レベルアップのカードの N を白に。カメラを引く案を ?cam= で比較（80% / 90%）。
22. タイトル画面に HOW TO PLAY（遊び方）を追加し、全仕様をまとめた。図鑑の属性タブを遊び方に移動。爆発と MIRACLE の音の重なりを抑制。リザルトのダメージ内訳が iPhone で抜ける問題に対処。
23. 1 戦を 10 分 → 7 分に（予定表と難易度の曲線を 0.7 倍に縮めた。ボスの時刻は分単位に切り捨て）。
24. チャーム「シトリン」を追加し、宝石を追加するときの手順をまとめた。
25. 宝石の絵を実際のカット（ブリリアント・ステップ・カボション・原石風）に描き直した。

---

## 9. 保留中・未着手の案（次に話題になりそうなこと）

- **ヴァンサバの要素で未実装のもの**：合体（ユニオン。2 武器を合体させて枠を空ける）、リミットブレイク（Lv8 以降も強化）、アドベンチャー、協力プレイ。合体とリミットブレイクは、ユーザーに提案済みで保留中。
- **秘宝の候補で保留したもの**：「経験値ジェムを取るとレベルが上がらず、ジェムが敵に飛ぶ爆弾になる＋全宝箱で進化」。やり込み用の縛りとして将来の候補。
- ガチャについて、ユーザーは「まだ考えている」段階から今の原石ガチャ＋交換所に落ち着いたが、今後また見直す可能性がある。
- 雑魚の硬さの連動（trashMul）とブレイクは調整中。強すぎ・弱すぎの報告があれば、trashMul の √・基準 3500・上限 10、ブレイクの 0.34・1.4・4 秒、ボス戦中の出現 0.65 倍を調整する。
